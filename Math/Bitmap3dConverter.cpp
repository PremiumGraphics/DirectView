#include "Bitmap3dConverter.h"

using namespace Crystal::Math;

template<size_t N>
std::vector<Box> Bitmap3dConverter::toBoxes(const Bitmap3d<N>& bmp)
{
	//bmp[0][0][0];
	return std::vector<Box>();
}